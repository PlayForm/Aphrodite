aphrodite-tool-guide SKILL.md needs three updates (tested in aphrodite-compress-aggressive session, skill_manage can't cross profiles from default):
(1) Frontmatter: related_skills add aphrodite-iterate-release, version → 1.1.0
(2) Add "When to Load" section after description with trigger conditions (need tool reference, debugging CCR markers, chaining tools, master-worker pattern). Note pairings: aphrodite-dev-workflow for dev, aphrodite-iterate-release for release cycle.
(3) Document multi-line CCR format with centers: Rust proxy produces 3-line format: [preview] + [type: meta;center=X] + <<<CCR:hash|type|size>>>. Python _ccr_marker() doesn't support center param yet (Rust-only feature). Tool relay endpoint is /tool/relay (not /tool_relay). Center travels via _ccr_center param through Rust tool relay path only.
Repo canonical skill file at: /Volumes/CORSAIR/Developer/macOS/Application/PlayForm/HermesCompress/skills/hermes/aphrodite-tool-guide/SKILL.md
§
CCR center support (v0.5.84+): Rust proxy smart_marker/format_ccr_output produces 3-line multi-line format: Line1=preview, Line2=[type: ln=N,fns=X;center=code_rust], Line3=<<<CCR:hash|type|size>>>. Center travels via _ccr_center param through /tool/relay endpoint only (NOT /ccr/create). Python _ccr_marker() and _compress_handler don't support center yet — Rust-only feature. Tool relay path is /tool/relay (not /tool_relay). Full proxy.rs test: 71KB → marker with ;center=code_rust in line2. Retrieval returns raw content as expected.