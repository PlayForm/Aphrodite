name: hermes-compress
version: 1.0.2
description: "Headroom suite: compress, retrieve, stats, proxy mgmt + native middleware + sandbox recovery + proxyless CCR mode"
toolsets:
  - compression
provides_tools:
  - headroom_compress
  - headroom_retrieve
  - headroom_stats
  - headroom_proxy_start
  - headroom_proxy_stop
  - headroom_proxy_status
provides_hooks:
  - llm_request