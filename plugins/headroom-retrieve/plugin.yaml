name: headroom-retrieve
version: 0.5.0
description: "Full headroom MCP suite — compress, retrieve, stats. Works with token proxy or direct+shim."
provides_tools:
  - headroom_compress
  - headroom_retrieve
  - headroom_stats
