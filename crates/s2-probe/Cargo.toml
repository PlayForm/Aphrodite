[package]
name = "s2-probe"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "s2-probe: S2-geometry level generator for per-task context shapes - graphs them and stores every generated shape in an append-only shape database; experimental probe behind the future `aphrodite doctor` context-shape viewer"
publish = false

[dependencies]
s2 = "0.1.0"
