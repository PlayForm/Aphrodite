# FORMAT-ALIGN.md - byte-identical formatting Development ↔ Current

Date: 2026-09-18 · Branch: `Development` · Repo: PlayForm/Aphrodite
Goal: every shared file's formatting on `Development` matches what the same
file on `Current` formats to under the SAME toolchain + config, with zero
formatting-only diffs between the branches.

## Toolchain (verified)

| Tool                         | Version                                       |
| ---------------------------- | --------------------------------------------- |
| rustfmt (nightly-2026-05-01) | rustfmt 1.9.0-nightly (f53b654a88 2026-04-30) |
| ruff                         | 0.15.17                                       |
| prettier                     | 3.9.6                                         |

## Pre-format delta (Development, before this pass)

| Formatter                                           | Result                                                                                      |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| `cargo fmt --all -- --check` (nightly)              | 0 files - already clean (commit `21960a5` had applied the nightly pass)                     |
| `ruff format --check` (whole repo, vendor excluded) | 13 files flagged → 12 non-vendor (11 superproject `.py` + `plugins/aphrodite/_bindings.py`) |
| `prettier --check` README.md docs .hermes           | 0 files - already clean                                                                     |

## Format-all result

- rustfmt: no files needed changes (already nightly-canonical).
- ruff format: **12 files reformatted** - bench/agents/mock_upstream.py,
  wire_compat.py, bench/conversational/conversations.py, harness.py,
  visualize.py, bench/corpus/code_python.py, gen_corpus.py, verify_labels.py,
  bench/proxy/bench_proxy.py, tests/test_directives_materialize.py,
  tests/test_layout_check.py, and `plugins/aphrodite/_bindings.py` (see
  restoration note below - it was reverted, NOT kept).
- prettier: no files needed changes.

## _bindings.py restoration (machine-generated artifact)

`plugins/aphrodite/_bindings.py` is machine-generated by the FFI codegen
pipeline (cbindgen → ctypesgen → codegen/finalize_bindings.py, copy-on-change
byte-stability). The format pass had touched it (an auto-committer even
swept the formatted copy into the plugin submodule as `ec73dc6`). It was
**restored** from git (submodule commit `455732d`, the `Development` gitlink):

- on-disk sha256 `5dc2770e…` == gitlink `455732d:_bindings.py` (byte-identical);
- codegen pipeline re-verified: `python3 crates/aphrodite-hermes/codegen/test_finalize_bindings.py` → 23 tests OK;
- submodule reset back to the recorded gitlink (final submodule HEAD `b54d388` has a tree byte-identical to `455732d`, so the artifact is byte-stable at HEAD);
- **config guard added**: `ruff.toml` `extend-exclude` now lists
  `plugins/aphrodite/_bindings.py` (committed `fdd4192`), so no future ruff
  format/lint pass touches it. `crates/aphrodite/templates/` stays excluded
  (byte-identical plugin shim; drift-guard `SHIM-IDENTICAL` re-verified).

## Byte-identity sample verification (15 shared files)

Method: for each file, extract `Current:<path>` and `Development:<path>` into
twin temp dirs, run the SAME formatter with the SAME config (rustfmt.toml /
ruff.toml / .prettierrc copied into both), diff the OUTPUTS.

| File                                  | Outputs           | Notes                                                              |
| ------------------------------------- | ----------------- | ------------------------------------------------------------------ |
| crates/aphrodite/src/config.rs        | byte-identical    | identical content both branches                                    |
| crates/aphrodite/src/session.rs       | byte-identical    | identical content                                                  |
| crates/aphrodite/src/catalog.rs       | byte-identical    | identical content                                                  |
| crates/aphrodite/src/prefetch.rs      | byte-identical    | identical content                                                  |
| crates/aphrodite/src/poll_worker.rs   | byte-identical    | identical content                                                  |
| crates/aphrodite/build.rs             | byte-identical    | identical content                                                  |
| crates/aphrodite/src/config_loader.rs | content-only diff | both sides canonical; diff = new features                          |
| crates/aphrodite/src/state.rs         | content-only diff | both sides canonical                                               |
| crates/aphrodite/src/preview.rs       | content-only diff | both sides canonical                                               |
| crates/aphrodite/src/proxy.rs         | content-only diff | both sides canonical                                               |
| plugins/**init**.py                   | byte-identical    | identical content                                                  |
| plugins/aphrodite/**init**.py         | content-only diff | submodule gitlinks differ (c0bc01d vs 455732d); formatting aligned |
| README.md                             | content-only diff | v1.4.5→v1.4.6 badges + install-section rewrite                     |
| docs/proxy/architecture.md            | byte-identical    | identical content                                                  |
| crates/aphrodite/README.md            | byte-identical    | identical content                                                  |

Every pair: `rustfmt --check` / ruff-canonical on BOTH sides; for the
content-divergent pairs the formatted-output diff has raw == whitespace-ignored
line counts (zero whitespace-only hunks → no formatting divergence remains).
The struct-literal flag is confirmed: `config_loader.rs:34` emits
`Self { raw:table, overrides:HashMap::new() }` (space_after_colon=false +
struct_lit_single_line=true under nightly-2026-05-01).

## Config investigation

- `rustfmt.toml`: was NOT byte-identical - `Current` carried a 5-line
  `ignore` entry for `crates/aphrodite/templates/` (shim byte-identity guard)
  that `Development` lacked. **Ported it** (working tree; auto-committer sweep
  pending). `cmp Current:rustfmt.toml Development:rustfmt.toml` now passes.
- `ruff.toml`: `[format]` section already ported; this pass added the
  `_bindings.py` extend-exclude (above). Current's committed ruff.toml predates
  the `[format]` port - Development's is the target config (port direction is
  Development ← Current for [format], plus the generated-file guard).
- `.prettierrc`: byte-identical between branches (verified).
- Toolchain pin: VSCode override `nightly-2026-05-01` matches the rustfmt used
  here (1.9.0-nightly, 2026-04-30 build).

## Final all-clean confirmation (post-alignment, post-restore)

- `rustup run nightly-2026-05-01 cargo fmt --all -- --check` → exit 0
- `ruff format --check --config ruff.toml . --exclude vendor` → 52 files already formatted, exit 0
- `npx prettier --check README.md docs/**/*.md .hermes/**/*.md` → all clean, exit 0
- `plugins/aphrodite/_bindings.py` sha256 `5dc2770e…` == gitlink (byte-stable)
- codegen pipeline test: 23/23 OK; plugin shim drift-guard: SHIM-IDENTICAL

## Out of scope / left as-is

- `vendor/` (headroom, rtk submodules), `target/`, `.hermes/tmp/` - never formatted.
- Content-level branch evolution (config_loader/state/preview/proxy feature
  diffs, submodule **init**.py redesign, README badge/version bumps) is
  intentional `Development`-ahead content, not formatting.
- No commit made by this task; the auto-committer sweeps (it already landed
  `00b0506` = the 11 superproject .py reformats, `fdd4192` = the ruff.toml
  extend-exclude guard, and `b54d388` = the pristine _bindings.py restore).
