# HANDOFF - 2026-09-17 session end (Development, binary 1.4.6 / plugin 2.1.4)

Status: session handoff for the Aphrodite monorepo. The SIGSEGV is fixed and
hardened, the FFI pipeline is generated + validated, the plugin is enabled and
working, **Issue #11 (preview-collapse bug family) has LANDED (WS1+WS2+WS4) with
the full verification sweep green**, and the FFI CI fixes (ruff + stale
self-test + trigger paths) are in. Everything below tells a new session what is
done, what is pending, and how to continue.

---

## 1. What is DONE (verified this session)

### 1.1 SIGSEGV fixed + FFI hardened (the reason this session started)

- Root cause: ctypes `restype` defaulting to `c_int` truncated 64-bit pointers
  from the dylib; `aphrodite_hermes_materialize_directives` was missing from the
  setup block.
- Committed fix: `dfa0a92` (parent) / `06c3bf3` (submodule).
- Hardening (shim, in `plugins/aphrodite/__init__.py`, all preserved):
  `_REQUIRED_VOID_P` assertion, `_probe_dylib` subprocess sentinel, `_read_str`
  NULL guard, `_check_version_published` (warns on missing release), per-hook
  try/except registration, `_call_json` forced `restype=c_void_p` + same-handle
  free (F4), hot-reload lock + copy + mtime. Post-mortem:
  `.hermes/notes/HERMES-UPDATE-ERROR-PLUGIN-SIDE.md`.
- Repro: `python3 repro.py` (6 threads x 300 hammer) SURVIVED 2/2.

### 1.2 FFI codegen pipeline (the main new architecture)

- `crates/aphrodite-hermes/build.rs` runs cbindgen 0.29 -> `aphrodite_hermes.h`
  -> ctypesgen (2.7.4-27202 installed) -> `codegen/finalize_bindings.py` ->
  committed `plugins/aphrodite/_bindings.py` (copy-on-change, byte-stable).
- `finalize_bindings.py` (409 lines): AST-based validate() (NO exec), strips
  ctypesgen preamble dead code, `__all__ = ['bind_to']`, rewrites restypes to
  `c_void_p`, argtypes `[c_char_p, c_char_p]` for dispatch/call_hook,
  `free_string [c_void_p]`, fork detection (warn + graceful skip for
  pypdfium2/unknown shapes).
- Artifact: 135 lines (was 981), purely declarative, `bind_to(dylib)` replays
  onto the hot-reload CDLL handle; plugin falls back to the manual setup block
  if the artifact is absent/corrupt (`_manual_ffi_setup`).
- Graceful degradation: missing ctypesgen/cbindgen/config -> `cargo:warning` +
  skip, committed artifact stays in effect. Contract violation with tools
  present -> build panic (by design).
- New tests: `crates/aphrodite-hermes/codegen/test_finalize_bindings.py`
  (23/23), `test_free_string_round_trip_symmetric` (46/46 aphrodite-hermes).
- Static checker: `Maintain/check_ffi_contract.py` (AST-based, exit 0/1/2),
  self-tests `Maintain/tests/test_check_ffi_contract.py` (13/13, 50 asserts -
  rewritten + argtypes-count gap closed), CI workflow
  `.github/workflows/ffi-check.yml` (trigger paths now cover build.rs,
  cbindgen.toml, codegen/**, crates/aphrodite-hermes/**, plugins/aphrodite/**),
  package.json `Check:FFI` script.
- Reports: `.hermes/notes/DEVELOP.md`, `RESEARCH-UPSTREAM.md`,
  `RESEARCH-FORK-INTEGRATION.md`, `CI-FFI-CHECK.md`, `DOCS-UPDATE.md`,
  `CHECKER.md` (sigserve), `PAIR-A1.md`, `PAIR-A2.md`.

### 1.3 Architecture cleanup (this session)

- Skills: moved to `.hermes/skills/` (dev-side only); plugin skill-shipping
  machinery REMOVED (`skills.rs`, `aphrodite_hermes_list_skills` FFI, tests).
- Directives: embedded in `crates/aphrodite/src/builtin_directives/` (7 files
  incl. lazy-eval.md), materialized by the binary via
  `aphrodite_hermes_materialize_directives` into `~/.hermes/aphrodite/directives/`
  at registration; never overwrites user files.
- Runtime home: `~/.hermes/aphrodite/` holds `aphrodite.toml`, `binaries/`,
  `directives/`, `hotreload/`; plugin dir is a pure loader (symlink to the
  checkout). Layout self-heal: `plugins/aphrodite/layout_check.py` +
  `layout_schema.json` (creates missing dirs, repairs symlinks, best-effort).
- Removed: `Maintain/install.sh/.ps1/.bat` (user), `profiles/` tree + symlinks,
  `.githooks/` entirely (user, 2026-09-17), S2 navigation subsystem
  (`crates/s2-probe`, `crates/s2-navigate`, `navigate.rs`, `aphrodite_navigate`
  tool, `navigation` feature), `CLAUDE.md` (commit 24c8258).
- Phantom self-referential gitlink (mode 160000 entry named after the submodule
  itself): auto-committer signature, NOT a hooks issue. Fix:
  `git rm --cached plugins/aphrodite`. Same class seen in the Documentation
  repo (`Module/Astro`) - cleanup opportunity there.

### 1.4 Versions + docs (1.4.6 / 2.1.4)

- Binary 1.4.6 everywhere (both Cargo.tomls + dep pin + package.json + README
  badge), plugin 2.1.4 (plugin.yaml + install_message), BINARY_VERSION 1.4.6.
- Release dylib built and copied to `~/.hermes/aphrodite/binaries/`.
- `.hermes/uml/` 01-11 + README rewritten to match current source
  (commit d87bb8c); `.hermes/AGENTS.md` updated (v2.1.4);
  `RELEASE-METHODOLOGY.md` example tags -> v2.1.4.
- TAXONOMY.md v0.2 + 4 classification pass files (HPC system) in
  `.hermes/classification/` - intentional 2026-09-17 snapshot, do not touch.

### 1.5 Issue #11 preview-collapse bug family - LANDED (WS1 + WS2 + WS4)

- Full record: `.hermes/notes/ISSUE-11-LANDED.md` (what landed, test counts,
  battery status, deferrals, CI status, sweep table). Pair reports:
  `PAIR-B1.md` (WS1 + WS2-tools), `PAIR-B2.md` (WS2-preview + WS4),
  `PAIR-B-FIX.md` (preview test-race fix).
- WS1 (`crates/aphrodite-hermes/src/tools.rs`): `ok` collapse deleted,
  success-string arm + priority-key arm guarded (single-key only),
  caller-hint-wins in `compress_into`.
- WS2: honest `total_count` (tools.rs: `matches_text` normalization, `take(20)`
  cap removed, zero/count-only totals surface, no `total_count`-only hijack);
  honest build arm + error/linter/log arms (`crates/aphrodite/src/preview.rs`:
  line-based tallies, failing-test-run honesty, first-error/lint/tail arms).
- WS4: `preview_max_chars` wired end-to-end (env > TOML > default 120, cap at
  the `build_preview` choke point, char-boundary-safe with closing `]` + `…`
  preserved; applied at startup, on `/reload`, and in dylib init).
- Tests before -> after: aphrodite 348 -> 389; aphrodite-hermes 46 -> 52;
  codegen self-tests 23 (unchanged); checker self-tests 12 -> 13.
- Sweep (2026-09-18): checker PASS 0 violations; repro SURVIVED; cargo tests
  389 + 52 green; codegen 23/23; checker self-tests 13/13 (50 asserts);
  drift-guard identical; ruff only the 2 known pre-existing perf-probe errors;
  prettier clean. **Battery-after summary PENDING** (C1's
  `ISSUE-11-BATTERY-AFTER.md` not found - re-run the 95-row battery against the
  rebuilt dylib to record the defect-rate drop).
- Deferred: WS3 proxy-pipeline parity -> 1.5.0 (see 2.2).

### 1.6 FFI CI fixes - LANDED (Pair A)

- Ruff on generated `_bindings.py` (~30+ errors): `ruff.toml` per-file-ignores
  for the machine-generated artifact + `# noqa: N812` on the alias import +
  top-level `extend-exclude` for `crates/aphrodite/templates`; verified on ruff
  0.15.17 and 0.16.8 (CI version).
- Stale checker self-test `test_cli_exit_codes_and_missing_files` (11/12):
  rewritten 13/13, both ground-truth halves pinned; checker hardened with
  `[wrong-argcount]` + `[unknown-export-argtypes]` violation classes.
- ffi-check.yml trigger gap closed (build.rs, cbindgen.toml, codegen/**,
  crates/aphrodite-hermes/**, plugins/aphrodite/** on push + PR).
- Pre-existing CI blockers unchanged (NOT FFI): `kompress_parity` E0432
  (vendor/headroom `ml`-gate) + clippy doc-lazy-continuation lib.rs:539.

## 2. What is PENDING (the new session's queue)

### 2.1 Release: Aphrodite/v1.4.6 does NOT exist yet on GitHub

- Latest published is v1.4.5. Bumps are local only; `_check_version_published`
  now warns instead of 404ing. The release ceremony (Phase B sync-back, tag,
  Finalize 12 assets, BINARY_VERSION last at tag time) is pending per
  `.hermes/notes/RELEASE-METHODOLOGY.md`.
- **NEW - branch-identity audit step:** before tagging, verify the parent
  repo and the `plugins/aphrodite` submodule are BOTH on `Development` (branch
  discipline is manual now that `.githooks/` is gone; a tag on the wrong branch
  cannot be re-pointed). Also confirm no phantom gitlink entry
  (`git ls-files -s plugins/aphrodite` must show mode 160000, and `git
submodule status` must show the pinned SHA with no `-`/`+` prefix).

### 2.2 1.5.0 backlog (from research + notes)

- WS3 proxy-pipeline parity (deferred from Issue #11 fix; `proxy.rs` parallel
  preview arms).
- Structured hook payloads (hermes-agent sends rich kwargs: conversation_history
  list, tool_name/args/result/duration_ms, command/output, session_id/model/
  platform; plugin flattens with `default=str` - Rust reads keyed strings).
- Consider `proxy_health` export: currently dead for the plugin (HTTP /health
  probe used instead; Rust export is TCP-only).
- Rust teardown export for authoritative dylib unload (roto/rust-ffi-guide
  pattern).
- Stress-test the pipeline against rust-bindgen corpus shapes (function-pointer
  returns, nested pointers, opaque structs) before adding new exports.
- CI: exercise the real codegen path (install ctypesgen; byte-identity check
  vs committed `_bindings.py`) - today CI tests only the fallback path.

### 2.3 Housekeeping

- Documentation repo: `Module/Astro` phantom gitlink (same auto-committer
  class) - cleanup there.
- `.hermes/notes/` now holds the session's durable records: DEVELOP.md,
  RESEARCH-UPSTREAM.md, RESEARCH-FORK-INTEGRATION.md, CI-FFI-CHECK.md,
  DOCS-UPDATE.md, HERMES-UPDATE-ERROR-_, ISSUE-11-_, PAIR-A1/A2/B1/B2/B-FIX.md,
  ISSUE-11-LANDED.md, RELEASE-METHODOLOGY.md. All anonymized (repo-relative
  paths, zero local absolute paths).
- Battery-after record: run C1's 95-row preview battery on the rebuilt dylib
  and write `ISSUE-11-BATTERY-AFTER.md` (defect-rate drop vs the 55% hook-path
  / 65% direct-path baselines in ISSUE-11-PREVIEW-BATTERY.md).

## 3. How to resume (new session checklist)

1. `cd` to the Aphrodite monorepo, branch Development; submodule
   `plugins/aphrodite` on Development (no `.githooks` anymore - branch
   discipline is manual).
2. Verify the plugin is enabled and the dylib loads (hermes logs show
   `aphrodite-hermes dylib loaded`); if binaries missing, `_ensure_binaries`
   auto-fetches.
3. Sanity sweep: `python3 Maintain/check_ffi_contract.py` (PASS),
   `python3 sigserve/repro.py` (SURVIVED), `cargo test -p aphrodite` (389),
   `cargo test -p aphrodite-hermes` (52),
   `python3 crates/aphrodite-hermes/codegen/test_finalize_bindings.py` (23),
   `python3 Maintain/tests/test_check_ffi_contract.py` (13/13), drift-guard
   `diff -q plugins/aphrodite/__init__.py
crates/aphrodite/templates/__init__.py`.
4. Next task in queue: **2.1 release ceremony** (v1.4.6 tag + Finalize 12
   assets + BINARY_VERSION last, incl. the branch-identity audit step), then
   2.2 (1.5.0: WS3 first) and 2.3 housekeeping + battery-after record.
5. Working style: user commits as they go (auto-committer sweeps); do NOT
   commit manually unless asked; no rebase ever; pairs of 2 subagents per task;
   scratch under the Temporary dir, durable records in `.hermes/notes/`.
