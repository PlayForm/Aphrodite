# HPC Classification - DEV: Engine + Build

See [TAXONOMY.md](TAXONOMY.md) for the code grammar.

**Scope:** `crates/aphrodite/**` (44 tracked), `crates/aphrodite-hermes/**` (8 tracked),
root build/config (18 tracked + 3 untracked-but-present), `vendor/**` (2 gitlinks).
**Total classified: 75 files/processes.**
**Pair partner:** `.hermes/**`, `bench/**`, `tests/**`, `docs/**`, `assets/**`, `references/**`,
plus `plugins/`, `.github/`, `Maintain/`, `directives/` (CUR passes). `.githooks/` was classified here originally but was **REMOVED 2026-09-17** (✝) - see CUR-release-infra-identity §1.1.
**Grammar:** `{K}{P}{L}-{N}[annotations]` - K=kind, P=phase (D/C/B/R), L=layer (1 engine, 2 hermes bridge, 3 plugin, 4 release infra/build, 5 identity, 6 meta, 7 vendor), N=seq.
Annotations: `→C` (Phase A push-down), `→D` (Phase B sync-back), `+tag`, `+bump`, `+float`, `+guard`, `∅` (never crosses), `@R` (ritual-only).
All files here are **halted processes**: opening a file resumes the process it halts; the table states what resumes.

---

## 1. Per-file classification

### 1.1 `crates/aphrodite/**` - core engine, layer 1 (44 files)

| Path                                     | HPC code          | Phase | Ceremony behavior                                                                                                                                                                                                                                                       | Halted-process resume note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------------------------------------- | ----------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib.rs`                             | `PD1-01 →C +bump` | D→C   | `D:crates/aphrodite/src/lib.rs --Phase A--> C:crates/aphrodite/src/lib.rs`; version rides release; ships in crates.io tarball (cdylib+rlib)                                                                                                                             | Compiling resumes the **whole engine**: C ABI surface (`aphrodite_init`…`aphrodite_destroy`, classify/compress/retrieve/…), all modules wired (`state`, `hooks`, `marker`, `resolve`, `flow`, `catalog`, `chain_split`, `session`, `prefetch`, `poll_worker`, `preview`, `stage2`, `struct_extract`, `config_loader`, `directives`; proxy-gated `config`/`proxy`/`retrieve`/`setup`). The single halt-point of the engine.                                                                                                          |
| `src/main.rs`                            | `PD1-02 →C +bump` | D→C   | `D:src/main.rs --Phase A--> C:src/main.rs`                                                                                                                                                                                                                              | Resumes the **proxy binary**: `--version`/`--help`, `aphrodite setup` subcommand, then multi-proxy mode (reads `aphrodite.toml`, spawns all listeners) or single-proxy CLI mode (`--mode cache --listen :9797`).                                                                                                                                                                                                                                                                                                                    |
| `src/state.rs`                           | `PD1-03 →C`       | D→C   | `D:src/state.rs --Phase A--> C:src/state.rs`                                                                                                                                                                                                                            | Resumes the **session's memory**: `AphroditeState` - inline store (LRU, byte-budgeted 256MB default), conv index, markers, tool-event ring, directive map. Every hook/tool call mutates this.                                                                                                                                                                                                                                                                                                                                       |
| `src/flow.rs`                            | `PD1-04 →C`       | D→C   | `D:src/flow.rs --Phase A--> C:src/flow.rs`                                                                                                                                                                                                                              | Resumes **per-turn context assembly**: first-turn `session_inject` (fallback const `SHIPPED_SESSION_INJECT`), catalog/directive/nudge sections under one hard `flow.budget_chars` cap; the single choke point all injection sites must route through.                                                                                                                                                                                                                                                                               |
| `src/config.rs`                          | `PD1-05 →C`       | D→C   | `D:src/config.rs --Phase A--> C:src/config.rs`                                                                                                                                                                                                                          | Resumes **CLI config**: clap `Cli`/`Command`/`MultiConfig`/`SetupArgs`; the one `env_bool` truthiness rule and `env_parse_warn` (warn-not-silent on malformed overrides); **no provider defaults baked in** (api_url/model come from env or TOML).                                                                                                                                                                                                                                                                                  |
| `src/config_loader.rs`                   | `PD1-06 →C`       | D→C   | `D:src/config_loader.rs --Phase A--> C:src/config_loader.rs`                                                                                                                                                                                                            | Resumes **TOML discovery**: `env > aphrodite.toml (./ then ~/.hermes/aphrodite/) > default`; never resolved relative to the binary/plugin dir; `Config::load()`/`apply_compression` feed the hermes bridge session init. Opening it re-runs the config-resolution halt.                                                                                                                                                                                                                                                             |
| `src/directives.rs`                      | `PD1-07 →C`       | D→C   | `D:src/directives.rs --Phase A--> C:src/directives.rs`                                                                                                                                                                                                                  | Resumes the **directive system**: embeds `builtin_directives/*.md` via `include_str!` (fresh installs get focus/foresight/ccr-handling/cleanup/explore/lazy/lazy-eval with no filesystem), disk `directives/` overrides, runtime swap via `aphrodite_directive`.                                                                                                                                                                                                                                                                    |
| `src/setup.rs`                           | `PD1-08 →C`       | D→C   | `D:src/setup.rs --Phase A--> C:src/setup.rs`                                                                                                                                                                                                                            | Resumes **bootstrap**: `aphrodite setup` writes `~/.hermes/aphrodite/` (binaries, config from the embedded `templates/aphrodite.toml` with `{cache_port}`/`{token_port}` placeholders, plugin manifest), self-verification + permission hardening, optionally launches the proxy. The repo-to-runtime-home transplant process.                                                                                                                                                                                                      |
| `src/proxy.rs`                           | `PD1-09 →C`       | D→C   | `D:src/proxy.rs --Phase A--> C:src/proxy.rs`                                                                                                                                                                                                                            | Resumes the **axum reverse proxy**: cache mode `:9797` (in-memory CCR), token mode `:9798` (SQLite CCR + tool relay), Chat Completions intercept/compress, `Secret` key wrapper that never leaks to logs.                                                                                                                                                                                                                                                                                                                           |
| `src/hooks.rs`                           | `PD1-10 →C`       | D→C   | `D:src/hooks.rs --Phase A--> C:src/hooks.rs`                                                                                                                                                                                                                            | Resumes the **compression pipeline**: `transform_tool_result` / `transform_terminal_output` - content-aware compression, essential-tool skip (`skill_view`, `read_file`, …), threshold gating, previews, `ToolCallMeta` telemetry.                                                                                                                                                                                                                                                                                                  |
| `src/catalog.rs`                         | `PD1-11 →C`       | D→C   | `D:src/catalog.rs --Phase A--> C:src/catalog.rs`                                                                                                                                                                                                                        | Resumes **catalog generation** (`toc`/`full` modes, by-type grouping, full 40-char hashes - truncated hashes are unresolvable).                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/chain_split.rs`                     | `PD1-12 →C`       | D→C   | `D:src/chain_split.rs --Phase A--> C:src/chain_split.rs`                                                                                                                                                                                                                | Resumes **command-chain splitting**: rewrites `a && b && c` with `__APHRODITE_SEG__` markers, splits output per segment; opt-in via `chain_split = false` shipped default (invariant D2).                                                                                                                                                                                                                                                                                                                                           |
| `src/marker.rs`                          | `PD1-13 →C`       | D→C   | `D:src/marker.rs --Phase A--> C:src/marker.rs`                                                                                                                                                                                                                          | Resumes **marker generation**: `<<<CCR:hash                                                                                                                                                                                                                                                                                                                                                                                \| type \| size>>>`blocks from TOML-driven templates;`normalize_hash`/`is_valid_ccr_hash` input hygiene. |
| `src/poll_worker.rs`                     | `PD1-14 →C`       | D→C   | `D:src/poll_worker.rs --Phase A--> C:src/poll_worker.rs`                                                                                                                                                                                                                | Resumes **auto-backgrounding**: slow tool calls (cargo/npm/pip/…) → `BgTask` + poll-worker marker; nudges via `pre_llm_call`, staleness expiry at `post_llm_call`.                                                                                                                                                                                                                                                                                                                                                                  |
| `src/prefetch.rs`                        | `PD1-15 →C`       | D→C   | `D:src/prefetch.rs --Phase A--> C:src/prefetch.rs`                                                                                                                                                                                                                      | Resumes **file preloading**: batch read paths into the CCR store (10MB cap), disk reads outside the state lock so one cold mount can't serialize the session.                                                                                                                                                                                                                                                                                                                                                                       |
| `src/preview/` (dir module)              | `PD1-16 →C`       | D→C   | `D:src/preview/mod.rs --Phase A--> C:src/preview/mod.rs` (atomized from `preview.rs` per REFACTOR-PLAN-1.5.0 §4: `detectors/` 17 shapes, `builders/` 22 arms, `line/` 13 predicates, `text/` 4 utils, `detect.rs`/`type.rs`/`input.rs`/`lang.rs`/`state.rs`/`tests.rs`) | Resumes **preview/type detection**: headroom classifier wrapper + Aphrodite-side semantic detector (`git`/`ls`/`test`/`grep`/`gitlog` shapes) that overrides flat `text`/`terminal` verdicts; `resolve_effective_type` hint-wins contract; `PREVIEW_MAX_CHARS` state. Facade must keep `build_preview`/`detect_type` names (lib.rs re-export contract).                                                                                                                                                                             |
| `src/resolve.rs`                         | `PD1-17 →C`       | D→C   | `D:src/resolve.rs --Phase A--> C:src/resolve.rs`                                                                                                                                                                                                                        | Resumes **marker resolution**: single-hash, nested (depth 5), cycle-safe, query-filtered expansion back to original content.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/retrieve.rs`                        | `PD1-18 →C`       | D→C   | `D:src/retrieve.rs --Phase A--> C:src/retrieve.rs`                                                                                                                                                                                                                      | Resumes the **HTTP `/retrieve` endpoint**: query/offset/limit windows, `source` attribution, truncation headers; `limit:0` returns exact original bytes (round-trip contract).                                                                                                                                                                                                                                                                                                                                                      |
| `src/session.rs`                         | `PD1-19 →C`       | D→C   | `D:src/session.rs --Phase A--> C:src/session.rs`                                                                                                                                                                                                                        | Resumes **session lifecycle**: `on_session_start` resets all state, turn counter, conv index, `archive_turn` (marker-at-end-of-turn).                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/stage2.rs`                          | `PD1-20 →C`       | D→C   | `D:src/stage2.rs --Phase A--> C:src/stage2.rs`                                                                                                                                                                                                                          | Resumes **stage-2 semantic reduction**: JSON minify+keys, build/log error/warn summary, diff hunk summary, code signature extraction; stateless ABI, no depth-aware retrieval yet (docstring-corrected).                                                                                                                                                                                                                                                                                                                            |
| `src/struct_extract.rs`                  | `PD1-21 →C`       | D→C   | `D:src/struct_extract.rs --Phase A--> C:src/struct_extract.rs`                                                                                                                                                                                                          | Resumes **code-structure extraction**: regex fn/struct/class signature maps for `[code_rust:3fns 2structs]` previews; `floor_boundary` UTF-8-safe truncation.                                                                                                                                                                                                                                                                                                                                                                       |
| `src/builtin_directives/focus.md`        | `DD1-01 →C`       | D→C   | `D:focus.md --Phase A--> C:focus.md`; also compiled **into** the binary via `include_str!`                                                                                                                                                                              | Resuming edits the shipped directive default; `Cargo.toml` NOTE forbids `*.md` excludes so these reach the crates.io tarball.                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/builtin_directives/foresight.md`    | `DD1-02 →C`       | D→C   | `D:foresight.md --Phase A--> C:foresight.md` (+ binary embed)                                                                                                                                                                                                           | Same: shipped behavioral default (anticipate/prefetch).                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/builtin_directives/ccr-handling.md` | `DD1-03 →C`       | D→C   | `D:ccr-handling.md --Phase A--> C:ccr-handling.md` (+ binary embed)                                                                                                                                                                                                     | Same: CCR-first retrieval doctrine.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `src/builtin_directives/cleanup.md`      | `DD1-04 →C`       | D→C   | `D:cleanup.md --Phase A--> C:cleanup.md` (+ binary embed)                                                                                                                                                                                                               | Same.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/builtin_directives/explore.md`      | `DD1-05 →C`       | D→C   | `D:explore.md --Phase A--> C:explore.md` (+ binary embed)                                                                                                                                                                                                               | Same.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/builtin_directives/lazy.md`         | `DD1-06 →C`       | D→C   | `D:lazy.md --Phase A--> C:lazy.md` (+ binary embed)                                                                                                                                                                                                                     | Same.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/builtin_directives/lazy-eval.md`    | `DD1-07 →C`       | D→C   | `D:lazy-eval.md --Phase A--> C:lazy-eval.md` (+ binary embed)                                                                                                                                                                                                           | Same.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `Cargo.toml`                             | `MD1-01 →C +bump` | D→C   | `D:Cargo.toml --Phase A--> C:Cargo.toml`; **version 1.4.6 rides the release**; must stay in lockstep with `crates/aphrodite-hermes/Cargo.toml`                                                                                                                          | Resumes **packaging**: crate-type cdylib+rlib, `[[bin]]`/`[[example]]` table, headroom-core path dep (pins vendor gitlink), `exclude = ["examples/","tests/","benches/",…]` with the hard-won `*.md` NOTE (builtin directives must ship in the tarball).                                                                                                                                                                                                                                                                            |
| `build.rs`                               | `SD1-01 →C`       | D→C   | `D:build.rs --Phase A--> C:build.rs`                                                                                                                                                                                                                                    | Resumes **compile-time stamping**: `APHRODITE_VERSION` (from `CARGO_PKG_VERSION` - bump rides the manifest, no separate edit), git hash, ISO build date (honors `SOURCE_DATE_EPOCH`), target triple, rustc version, profile.                                                                                                                                                                                                                                                                                                        |
| `templates/__init__.py`                  | `MB1-05 +guard`   | B     | **Never authored directly** - must stay byte-identical to `plugins/aphrodite/__init__.py` (setup.rs asserts; format the plugin first, then `cp`). No Phase-A/B arrow: it is the _mirror_, not the source                                                                | Opening it resumes the **drift-guard contract**; it is the embedded copy of the ctypes registration shim (dylib path resolution, hot-reload with per-PID reaping, proxy auto-launch).                                                                                                                                                                                                                                                                                                                                               |
| `templates/aphrodite.toml`               | `CB1-01 →C +tag`  | D→C   | `D:templates/aphrodite.toml --Phase A--> C:templates/aphrodite.toml`; **must be refreshed before release** (embedded by `setup.rs` via `include_str!` → the released binary ships whatever template was current at build); keep in sync with `aphrodite.toml.example`   | Resumes the **default-config writer**: `aphrodite setup` writes `~/.hermes/aphrodite/aphrodite.toml` from this, substituting `{cache_port}`/`{token_port}`; carries invariant D2 (`chain_split = false`) into every install.                                                                                                                                                                                                                                                                                                        |
| `tests/classifier_regression.rs`         | `TD1-01 ∅`        | D     | **∅ never crosses** (tests never ship; A3)                                                                                                                                                                                                                              | Resuming runs the classifier regression suite - Development's job, kept here in Phase B (reject test-free deletions).                                                                                                                                                                                                                                                                                                                                                                                                               |
| `tests/packaging.rs`                     | `TD1-02 ∅`        | D     | `∅`                                                                                                                                                                                                                                                                     | Resumes packaging tests (tarball contents, include_str! targets).                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `tests/roundtrip_regression.rs`          | `TD1-03 ∅`        | D     | `∅`                                                                                                                                                                                                                                                                     | Resumes compress/retrieve round-trip tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `tests/serde_json_features.rs`           | `TD1-04 ∅`        | D     | `∅`                                                                                                                                                                                                                                                                     | Resumes serde_json feature-pin tests (`preserve_order`/`arbitrary_precision`/`raw_value`).                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `examples/bench_01_corpus.rs`            | `TD1-05 ∅`        | D     | `∅` (bench example - never ships; A3)                                                                                                                                                                                                                                   | Resuming runs the corpus benchmark.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `examples/bench_02_threshold.rs`         | `TD1-06 ∅`        | D     | `∅`                                                                                                                                                                                                                                                                     | Resuming runs the threshold benchmark.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `examples/bench_03_retrieve.rs`          | `TD1-07 ∅`        | D     | `∅`                                                                                                                                                                                                                                                                     | Resuming runs the retrieve benchmark.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `examples/bench_04_ema.rs`               | `TD1-08 ∅`        | D     | `∅`                                                                                                                                                                                                                                                                     | Resuming runs the EMA benchmark.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `examples/bench_05_type_coverage.rs`     | `TD1-09 ∅`        | D     | `∅`                                                                                                                                                                                                                                                                     | Resuming runs the type-coverage benchmark.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `examples/bench_payloads/sample.rs`      | `TD1-10 ∅`        | D     | `∅`                                                                                                                                                                                                                                                                     | Resuming supplies the benchmark corpus payload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `README.md`                              | `DB1-01 →C`       | D→C   | `D:README.md --Phase A--> C:README.md`                                                                                                                                                                                                                                  | Resumes the crate doc process (install/usage for `cargo install aphrodite`).                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `.gitignore`                             | `G1-01 →C`        | D→C   | `D:.gitignore --Phase A--> C:.gitignore` (crate-local, not branch-owned identity)                                                                                                                                                                                       | Resumes crate-local hygiene (ignores `target/`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### 1.2 `crates/aphrodite-hermes/**` - Hermes bridge, layer 2 (7 files)

| Path                      | HPC code          | Phase | Ceremony behavior                                                                                                                                         | Halted-process resume note                                                                                                                                                                                                                                       |
| ------------------------- | ----------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib.rs`              | `PD2-01 →C +bump` | D→C   | `D:src/lib.rs --Phase A--> C:src/lib.rs`; version rides release                                                                                           | Resumes the **bridge dylib**: `aphrodite_hermes_*` C ABI (get_schemas/get_hooks/dispatch_tool/call_hook/materialize_directives/…), process-global `AphroditeState` initialized from `config_loader::Config` (env>TOML>default), poison-tolerant lock.            |
| `src/tools.rs`            | `PD2-02 →C`       | D→C   | `D:src/tools.rs --Phase A--> C:src/tools.rs`                                                                                                              | Resumes **tool dispatch**: the 13-tool registry (compress, retrieve, stats, catalog, search, directive, diff, files, prefetch, reclassify, rebuild, test, …) over the shared session state; workspace-confined 10MB path reads.                                  |
| `src/schemas.rs`          | `PD2-03 →C`       | D→C   | `D:src/schemas.rs --Phase A--> C:src/schemas.rs`                                                                                                          | Resumes the **tool-schema contract** (what the agent actually sees): one-line `_short_desc` rules, provider-sanitizer constraints (`enum`/`default`/`additionalProperties` survive; top-level `anyOf` does not); schema drift here is a released-product change. |
| `src/directives.rs`       | `PD2-04 →C`       | D→C   | `D:src/directives.rs --Phase A--> C:src/directives.rs`                                                                                                    | Resumes **directive materialization**: provisions the binary-embedded builtin set into `~/.hermes/aphrodite/directives/` (idempotent, user data wins); the plugin dir never holds runtime state.                                                                 |
| `src/bin/setup-helper.rs` | `PD2-05 →C`       | D→C   | `D:src/bin/setup-helper.rs --Phase A--> C:src/bin/setup-helper.rs`                                                                                        | Resumes the **install-helper**: the only `[[bin]]` `cargo install` distributes for this crate; prints the honest dylib story (cargo install can't ship cdylibs - use `aphrodite setup` or a source build).                                                       |
| `Cargo.toml`              | `MD2-01 →C +bump` | D→C   | `D:Cargo.toml --Phase A--> C:Cargo.toml`; version **hard-locked** to `aphrodite = 1.4.6` (path + version) - both manifests must bump in the same ceremony | Resumes **packaging**: cdylib+rlib, `[[bin]] setup-helper`; the path+version pin is a compile-time coupling between the two crates.                                                                                                                              |
| `README.md`               | `DB2-01 →C`       | D→C   | `D:README.md --Phase A--> C:README.md`                                                                                                                    | Resumes the crate doc process (bridge architecture, install).                                                                                                                                                                                                    |

### 1.3 Root build/config - layer 4 (release infra / build), 21 files

| Path                     | HPC code               | Phase | Ceremony behavior                                                                                                                                                          | Halted-process resume note                                                                                                                                                                                                                               |
| ------------------------ | ---------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Cargo.toml`             | `MB4-01 →C`            | D→C   | `D:Cargo.toml --Phase A--> C:Cargo.toml`                                                                                                                                   | Resumes **workspace resolution**: members aphrodite + aphrodite-hermes, `exclude = ["Maintain/dynamic_hooks"]`, shared workspace deps (serde/serde_json/bytes/thiserror/tracing), `rust-version = 1.88`, edition 2021.                                   |
| `Cargo.lock`             | `AB4-01 ∅ (untracked)` | B     | **Never crosses - gitignored** (`*.lock`); regenerated per line by `cargo`; not part of any transplant or tag                                                              | Resuming = `cargo` rewrites it. Not a repo process at all; exists only in working trees. `[AMEND-2]`                                                                                                                                                     |
| `rust-toolchain.toml`    | `CB4-01 →C`            | D→C   | `D:rust-toolchain.toml --Phase A--> C:rust-toolchain.toml`                                                                                                                 | Resumes **toolchain selection** for local builds: `channel = "1.96.0"` (note: CI rustfmt pins `nightly-2026-05-01` in workflows - the stable channel alone cannot honor `rustfmt.toml`'s unstable options).                                              |
| `rustfmt.toml`           | `CB4-02 →C +guard`     | D→C   | `D:rustfmt.toml --Phase A--> C:rustfmt.toml`; formatter contract transfers in Phase B                                                                                      | Resumes the **formatting contract**: nightly-only unstable options (`space_after_colon = false`, `unstable_features = true`), edition/style 2024, ignores `vendor/`, `target/`, codegen dirs.                                                            |
| `ruff.toml`              | `CB4-03 →C`            | D→C   | `D:ruff.toml --Phase A--> C:ruff.toml`; transfers in Phase B                                                                                                               | Resumes **Python lint/format**: line-length 100, py311, E/F/W/I/N/UP/B/C4/SIM, double quotes + space indent. (Methodology claims an `extend-exclude` for `templates/**` - **absent in the file**; see cross-cutting #1.)                                 |
| `deny.toml`              | `CB4-04 →C`            | D→C   | `D:deny.toml --Phase A--> C:deny.toml`                                                                                                                                     | Resumes the **dependency policy gate** (A0 checks: `cargo deny check`): license allowlist (CC0 + permissive only, no copyleft), advisory ignore `RUSTSEC-2024-0436`, `skip-tree` for the vendored `aphrodite-headroom-core`, crates.io-only sources.     |
| `pyproject.toml`         | `CB4-05 →C`            | D→C   | `D:pyproject.toml --Phase A--> C:pyproject.toml`                                                                                                                           | Resumes **pyright config**: python 3.11, basic type-checking.                                                                                                                                                                                            |
| `package.json`           | `MB4-02 →C +bump`      | D→C   | `D:package.json --Phase A--> C:package.json`; **version 1.4.6 rides the release** (matches both crate manifests); `prepare` hook (hooksPath + submodule sync)              | Resumes the **JS-side tooling manifest**: `Format*` scripts driving `Maintain/Format.sh`, prettier devDep, `packageManager: pnpm@11.18.0`.                                                                                                               |
| `pnpm-lock.yaml`         | `AB4-02 ∅ (untracked)` | B     | Never crosses - gitignored; regenerated per line                                                                                                                           | Resuming = pnpm rewrites it. Note: pins prettier **3.9.5** while `package.json` declares **3.9.6** - stale lock, invisible to git. `[AMEND-2]`                                                                                                           |
| `pnpm-workspace.yaml`    | `CB4-06 →C`            | D→C   | `D:pnpm-workspace.yaml --Phase A--> C:pnpm-workspace.yaml`                                                                                                                 | Resumes pnpm workspace resolution (excludes `**/Target/**`).                                                                                                                                                                                             |
| `uv.lock`                | `AB4-03 ∅ (untracked)` | B     | Never crosses - gitignored; regenerated per line                                                                                                                           | Resuming = uv rewrites it (python >=3.11, revision 3). `[AMEND-2]`                                                                                                                                                                                       |
| `.prettierrc`            | `CB4-07 →C +guard`     | D→C   | `D:.prettierrc --Phase A--> C:.prettierrc`; transfers in Phase B                                                                                                           | Resumes the **prettier contract**: tabs/4, LF, printWidth 100, per-language overrides (yaml/json 2-space, md prose preserve).                                                                                                                            |
| `.prettierignore`        | `CB4-08 →C`            | D→C   | `D:.prettierignore --Phase A--> C:.prettierignore`                                                                                                                         | Resumes the **formatter ownership map**: rustfmt owns `.rs`, ruff owns `.py`, shfmt owns `.sh`; vendor/plugins/target/Cargo.lock/pnpm-lock excluded; `.hermes/**/*.md` re-included.                                                                      |
| `.editorconfig`          | `CB4-09 →C`            | D→C   | `D:.editorconfig --Phase A--> C:.editorconfig`                                                                                                                             | Resumes editor defaults (LF, tab indent 4; rust/rs unset - rustfmt owns it).                                                                                                                                                                             |
| `.vscode/settings.json`  | `CB4-10 →C`            | D→C   | `D:.vscode/settings.json --Phase A--> C:.vscode/settings.json`; transfers in Phase B (B3 list)                                                                             | Resumes the **shared editor contract**: per-language formatters, `prettier.requireConfig`, excludes. (Methodology claims a rust-analyzer `rustup run nightly… rustfmt` override here - **absent in the file**; see cross-cutting #1.)                    |
| `.gitignore`             | `GB4-01 →C`            | D→C   | `D:.gitignore --Phase A--> C:.gitignore`; not branch-owned identity (only `.gitmodules`/workflows are)                                                                     | Resumes **repo hygiene**: ignores target/, lockfiles, `aphrodite.toml`/`aphrodite.local.toml`, runtime state (`state.db*`, `auth.json`), bench outputs; re-includes `.hermes/` and `.vscode/settings.json` (with the documented no-op-negation history). |
| `.gitattributes`         | `GB4-02 →C`            | D→C   | `D:.gitattributes --Phase A--> C:.gitattributes`; **`export-ignore` strips `.hermes/` + `.github/` from source archives** (auto-enforces never-ships at the archive layer) | Resumes **line-ending + export policy**: LF for text types, binary markers, archive exclusions.                                                                                                                                                          |
| `.lychee.toml`           | `CB4-11 →C`            | D→C   | `D:.lychee.toml --Phase A--> C:.lychee.toml`                                                                                                                               | Resumes **link checking** (docs CI): extensions, excludes (img.shields, localhost), `exclude_path` (vendor, .hermes, …).                                                                                                                                 |
| `.lycheeignore`          | `CB4-12 →C`            | D→C   | `D:.lycheeignore --Phase A--> C:.lycheeignore`                                                                                                                             | Resumes the link-check ignore list (PlayForm flaky URLs, vendor submodule paths, API endpoints).                                                                                                                                                         |
| `.gitguardian.yaml`      | `CB4-13 →C`            | D→C   | `D:.gitguardian.yaml --Phase A--> C:.gitguardian.yaml`                                                                                                                     | Resumes **secret scanning** (ggshield pre-commit): ignored paths (tests fixtures, `vendor/`), ignored detectors (jwt, generic-key).                                                                                                                      |
| `aphrodite.toml.example` | `CB1-02 →C`            | D→C   | `D:aphrodite.toml.example --Phase A--> C:aphrodite.toml.example`; keep in sync with `templates/aphrodite.toml` (see 1.1)                                                   | Resumes the **reference-config process**: the documented, fully-commented superset of every section the engine reads (env>TOML>default priority, loopback-only listeners, no provider defaults, template variable legend).                               |

### 1.4 `vendor/**` - third-party processes, layer 7 (2 gitlinks)

| Path              | HPC code       | Phase | Ceremony behavior                                                                                                                                                                                                                 | Halted-process resume note                                                                                                                                                                                      |
| ----------------- | -------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `vendor/headroom` | `V7-01 +float` | B     | **Gitlink, floated per line**: Phase A floats to the released tip; Phase B restores Development's own (B2 Action 7 restores `vendor/headroom`); parent `Cargo.toml` path dep `aphrodite-headroom-core 0.1.2` must track the float | Resumes the pinned **upstream compression engine fork** (headroom-core - classifier/transforms/CCR store backends); the fork boundary: Aphrodite overlays (`preview.rs` semantic detector) never touch vendor/. |
| `vendor/rtk`      | `V7-02 +float` | B     | Gitlink, floated per line; third-party, pinned                                                                                                                                                                                    | Resumes the pinned **rtk fork** (dev-0.43.0-rc lineage); not part of the engine build graph's visible path.                                                                                                     |

---

## 2. Notable ceremony interactions

1. **Version bumps ride the release (`+bump`) - three manifests, one number.** `crates/aphrodite/Cargo.toml`, `crates/aphrodite-hermes/Cargo.toml`, and root `package.json` all read `1.4.6` today. The hermes manifest pins `aphrodite = { path = "../aphrodite", version = "1.4.6" }` - a **path + version double pin**, so the two crates cannot bump independently (cargo fails resolution on mismatch). `build.rs` forwards `CARGO_PKG_VERSION`, so no fourth spot. This is the parent-side half of "binary 5 spots"; the plugin's `BINARY_VERSION` (partner scope) is bumped LAST and is the live download pointer.
2. **Template drift guard (`MB1-05 +guard`).** `templates/__init__.py` must be byte-identical to `plugins/aphrodite/__init__.py`; setup.rs asserts. Rule: format the plugin shim first, then `cp` to the template - never format the template directly. Because the shim is ruff-formatted, the current `ruff.toml` needs **no** `extend-exclude` for `templates/**` - the methodology's Formatter Contract section (see cross-cutting #1) is stale on this point.
3. **`templates/aphrodite.toml` must be refreshed before release (`CB1-01 +tag`).** It is compiled into the binary via `include_str!` (setup.rs) - the released binary ships whatever template was current at build time. It carries invariant D2 (`chain_split = false`) and the default proxy topology into every `aphrodite setup` install. It and `aphrodite.toml.example` must be updated as a pair (the example documents "the same structure").
4. **`config.rs` provider-default removal semantics.** The engine bakes **no provider defaults** (`api_url`/`model` come from `APHRODITE_API_URL`/`APHRODITE_MODEL` or per-proxy upstream config; `api_key` only from env/TOML). This is a _product decision_: the shipped config (`templates/aphrodite.toml`, `aphrodite.toml.example`) documents it, and the chain-split opt-in stays OFF there (invariant D2) - the ceremony must never let a Development experiment (e.g. `chain_split = true` in a dev config) leak into the template/example before a transplant. A config-first invariant: **dev-experimental keys stay out of both template files**.
5. **Vendor floats + `deny.toml` coupling.** `crates/aphrodite/Cargo.toml` depends on `aphrodite-headroom-core` by **path** into `vendor/headroom` with `version = "0.1.2"`; `deny.toml` `skip-tree`s that name. A vendor float that changes the vendored version requires the parent manifest version field to track it or the build fails - vendor float and crate manifest are a single ceremony atom (floated first in Phase A with the plugin sync; restored per-branch in Phase B).
6. **Lockfiles are ceremony-invisible (`AB4-* ∅`).** `Cargo.lock`/`pnpm-lock.yaml`/`uv.lock` are all gitignored - they never participate in Phase A/B, never land in the tag, and Current regenerates its own at build. Consequence: Current's Build.yml resolves fresh (fine for a workspace whose path deps pin the vendor float), but `pnpm-lock.yaml` (prettier 3.9.5) already drifts from `package.json` (3.9.6) with no git-visible signal. If reproducible node builds matter, tracking the lock would be the ceremony decision.
7. **`export-ignore` enforces never-ships at the archive layer.** `.gitattributes` strips `.hermes/` and `.github/` from GitHub source archives - the source-zip artifacts Build.yml produces for Current are automatically dev-scaffolding-free, doubling as a second enforcement of rule 5.

---

## 3. Cross-cutting observations

1. **Dev-only content hides inside the bulk-ship `crates/` dir - Phase A selective-pick hazard.** `crates/aphrodite/tests/**` (4 files) and `examples/bench_*` (6 files) are Development-only (∅) yet sit in the one directory the methodology ships as a unit ("crates/ … ships"). A mechanical `git merge --squash` carries them to Current; only the A2 Action 4 per-file review keeps them off. Recommendation: extend the A3 ships/never-ships table explicitly with `crates/aphrodite/tests/**` and `crates/aphrodite/examples/**`, and consider a `git ls-files crates/ | grep -E 'tests/|examples/'` gate in Action 12.
2. **The engine is source-in-repo, state-in-`~/.hermes`.** The halted processes in `crates/` _produce_ runtime state but never _hold_ it: `setup.rs` writes `~/.hermes/aphrodite/`, `config_loader.rs` reads it, hermes `directives.rs` materializes directives there, `templates/__init__.py` locates binaries/dylib there. The ceremony therefore never needs to touch user-home state - Current's runtime home is built by its own released binaries (`aphrodite setup`, plugin `download.sh`). Good separation; worth stating explicitly in the methodology so nobody tries to "ship" the runtime home.
3. **Layer map gap filled (`[AMEND-1]`):** the taxonomy had no home for root build/config processes; this pass places them at layer 4 (`CB4-*`/`MB4-*`/`GB4-*`/`AB4-*`), i.e. layer 4 = "release infra **+ build/config processes**". All root build configs are `→C` (they ship and transfer in both directions via the Phase B selection list: ruff/rustfmt/.vscode/…), none are identity (`G5-* ∅` stays reserved for `.gitmodules`/workflows/gitlink).
4. **Untracked-artifact convention (`[AMEND-2]`):** lockfiles are real build processes but not repo processes - they get `A` kind + `∅ (untracked)` so the taxonomy is honest that the ceremony has zero relationship with them. Future amendment candidates: `crates/aphrodite/templates/**` dual role (embedded-manifest vs config) could warrant an `E` (embedded) kind; today `M`/`C` with `+guard`/`+tag` suffices.
5. **The `crates/aphrodite/Cargo.toml` `exclude` NOTE is a packaging guard worth encoding in the taxonomy as `+guard`:** "do NOT add `*.md` to exclude" (builtin directives ship via `include_str!`; v1.3.8 shipped a broken tarball when they were stripped). The comment itself is the guard's documentation - ceremony A0 "gates green" should include a `cargo package --list` sanity check that `src/builtin_directives/*.md` appear.

---

## 4. Amendments (proposed, NOT yet written to TAXONOMY.md)

- `[AMEND-1]` Root build/config processes live on **layer 4** (release infra / build), extending the layer map: `CB4-*` configs, `MB4-*` manifests, `GB4-*` git-meta, `AB4-*` artifacts.
- `[AMEND-2]` Untracked-but-present generated artifacts (lockfiles) get kind `A` + `∅ (untracked)`: they never cross because git never stages them - each line regenerates its own.
- `[AMEND-3]` Embedded engine templates (`templates/aphrodite.toml`) are `C`-kind with `+tag` (must be refreshed before release); embedded shim copy (`templates/__init__.py`) keeps the taxonomy's existing `MB1-05 +guard`.
- `[AMEND-4]` `crates/aphrodite/tests/**` and `examples/bench_*` are Development-only **inside the bulk-ship `crates/` dir** - classified `TD1-* ∅`; see cross-cutting #2 for the Phase A hazard.
- **RELEASE-METHODOLOGY PART 5 (Formatter Contract) is stale against the actual tree.** Three claims don't match reality: (a) `ruff.toml` has **no** `extend-exclude` for `crates/aphrodite/templates/**` (the template is kept ruff-formatted, which is _why_ the byte-identity guard holds); (b) `.vscode/settings.json` has **no** rust-analyzer `rustup run nightly-2026-05-01 rustfmt` override - the only enforcement of the nightly-rustfmt contract lives in CI workflows; (c) `rustfmt.toml` ignores `vendor/`, `target/`, codegen dirs - not `templates/` (irrelevant there since rustfmt only formats `.rs`, but the doc says otherwise). Recommend updating PART 5 to match, or the ceremony will keep "verifying" a config that doesn't exist. Local `cargo fmt` on the stable `rust-toolchain.toml` (1.96.0) silently ignores the unstable options - the drift the contract exists to prevent is currently only caught by CI.

## 5. Verification

**Final count: 74 files/processes classified** (44 `crates/aphrodite`, 7 `crates/aphrodite-hermes`, 21 root build/config incl. 3 untracked lockfiles, 2 vendor gitlinks). No files outside this deliverable were modified. Nothing committed. `prettier --check` passes on this file.
