# HPC Classification - CUR: Plugin/Loader

See [TAXONOMY.md](TAXONOMY.md) for the code grammar.

**Pass:** CUR (Current-side pass) · **Scope owner:** plugin/loader processes
**Repo:** Aphrodite monorepo (Development) → `plugins/aphrodite` is a SUBMODULE → PlayForm/Aphrodite-Hermes
**Taxonomy:** `.hermes/classification/TAXONOMY.md` v0.1 · **Ceremony spec:** `.hermes/notes/release/RELEASE-METHODOLOGY.md`
**Date:** 2026-09-17 · **Submodule state:** branch `Development`, clean tree, HEAD `497671b` (v2.1.2-37-g497671b), NO `binaries/` dir on disk

Every file below is a **halted process** - opening it resumes it. The plugin submodule is the
_loader/downloader_ face of Aphrodite: 15 tracked files, zero CI, zero workflows, pure Python shim +
manifest + downloaders + a self-heal module. The submodule is a gitlink that **floats** (+float) inside
the parent; its tree syncs **FIRST** in both ceremony directions (Phase A1 / Phase B1), bottom-up.

---

## 1. Per-file classification

### 1.1 plugins/aphrodite (tracked files)

| Path                                                     | HPC code                                          | Phase                               | Ceremony behavior (annotations + commutative diagram)                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Halted-process resume note (what starts when opened)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------- | ------------------------------------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `plugins/aphrodite/__init__.py`                          | `PB3-01 →C +tag` _(canonical, TAXONOMY §4)_       | Both                                | Ships in every Phase A snapshot; formatter-aligned fixes return via Phase B. **Template drift guard**: must stay byte-identical to `crates/aphrodite/templates/__init__.py` (setup.rs `include_str!` + drift-guard test - VERIFIED identical, 43467 B both sides). Part of the Tag composition. `D:{__init__.py} --Phase A--> C:{__init__.py'}` · `C:{__init__.py} --Phase B--> D:{__init__.py''}`                                                                                                          | Resumes the **loader process**: Hermes `exec_module()`s it once per Hermes home (root + each profile); it resolves the dylib (env override → canonical `~/.hermes/aphrodite/binaries` → legacy plugin-dir → `target/release`), auto-fetches missing binaries via `download.sh` (`_ensure_binaries`, honors `APHRODITE_NO_AUTO_DOWNLOAD`), loads `libaphrodite_hermes` via ctypes with mtime hot-reload (fresh-path copies `<base>.<pid>.<gen>`), reaps stale hotreload copies, runs layout self-heal (`check_and_heal`), materializes directives from the dylib, registers **6 hooks + 13 tools** (+ opt-in context engine), and starts/reuses the `:9797`/`:9798` proxy pair. |
| `plugins/aphrodite/plugin.yaml`                          | `MB3-01 →C +bump +tag` _(amendment 1)_            | Both                                | **Version rides the release**: `version: 2.1.3` == plugin tag `v2.1.3`; bumped on Development pre-A0, shipped in the A1 squash snapshot, tagged at ceremony end (plugin tag first, before parent tag). Shared content - transfers both directions. `D:{plugin.yaml} --Phase A--> C:{plugin.yaml'}` · `C:{plugin.yaml} --Phase B--> D:{plugin.yaml''}`                                                                                                                                                       | Resumes the **manifest process**: Hermes reads it to discover the plugin surface - 13 tools (`provides_tools`), 6 hooks (`provides_hooks`), `provides_context_engine: true`, `min_hermes_version: 0.16.0`; declares the installer message and the "skills NOT shipped" rule.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `plugins/aphrodite/BINARY_VERSION`                       | `MC3-01 +tag+guard` _(canonical, TAXONOMY §2/§4)_ | Current (conceptually Current-only) | **LIVE download pointer, bumped LAST** in any ceremony: value `1.4.3` pairs the plugin with binary release `Aphrodite/v1.4.3`; bumping before the release assets exist breaks every download. `+guard`: strict semver validation in both downloaders + `_check_version` handshake against the loaded dylib. Note: the binary line (1.4.3) and plugin line (2.1.3) are **independent release cadences** - this manifest is the coupling. `D:{BINARY_VERSION} --Phase A (bumped LAST)--> C:{BINARY_VERSION'}` | Resumes the **version-pin process**: `download.sh`/`download.ps1` read it as version source #1 (BINARY_VERSION → Cargo.toml → GitHub latest); `__init__._check_version` warns when the loaded dylib's version disagrees with it (stale/newer dylib = changed JSON contract).                                                                                                                                                                                                                                                                                                                                                                                                   |
| `plugins/aphrodite/download.sh`                          | `SB3-01 →C +guard` _(amendment 2)_                | Both                                | Ships on Current (users install from Current and must fetch binaries). `+guard`: `validate_version` regex rejects a malformed BINARY_VERSION (e.g. stray `1                                                                                                                                                                                                                                                                                                                                                 | 1.3.9`editor artifact) before it builds`releases/download/Aphrodite%2Fv{V}`; SHA-256SUMS verification + magic-byte check (ELF/Mach-O/PE) on every fetch. Fixes sync back in Phase B. `D:{download.sh} --Phase A--> C:{download.sh'}`·`C:{download.sh} --Phase B--> D:{download.sh''}`                                                                                                                                                                                                                                                                                                                                                                                          | Resumes the **binary auto-downloader**: resolves version, auto-detects target triple, downloads proxy binary + dylib into the canonical runtime home `~/.hermes/aphrodite/binaries` (BINARY_DIR default), restores `.bak` on failure. Invoked programmatically by `_ensure_binaries` with `BINARY_DIR` pinned so ambient env can't redirect the fetch back into the plugin tree. |
| `plugins/aphrodite/download.ps1`                         | `SB3-02 →C +guard` _(amendment 3)_                | Both                                | Native PowerShell mirror of download.sh - same ceremony role, same `+guard` (semver validation + checksum + magic-byte), same version-resolution order. `D:{download.ps1} --Phase A--> C:{download.ps1'}` · `C:{download.ps1} --Phase B--> D:{download.ps1''}`                                                                                                                                                                                                                                              | Resumes the **Windows-native downloader**: `pwsh ./download.ps1 [-Version] [-Target]`; no Git Bash/WSL needed; same canonical `~/.hermes/aphrodite/binaries` destination.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `plugins/aphrodite/layout_check.py`                      | `PD3-02 →C` _(canonical, TAXONOMY §4)_            | Development home, ships             | **Self-heal module**: ships with the plugin because its runtime home is the USER side (`~/.hermes`), not the repo. Never raises (every failure degrades to report-only); stdlib-only so tests run without Hermes. `D:{layout_check.py} --Phase A--> C:{layout_check.py'}`                                                                                                                                                                                                                                   | Resumes the **layout self-heal process** at every plugin registration: `check_and_heal()` compares `~/.hermes` against `layout_schema.json` - relocates misplaced `binaries/`/`aphrodite.toml`/`ccr.db` out of the plugin dir (copy + byte-verify + remove), quarantines stale plugin-source copies to `.stale-backup/`, recreates the `~/.hermes/plugins/aphrodite` symlink, refuses to touch a git checkout (dev-managed), protects newer-than-canonical binaries (warn+skip).                                                                                                                                                                                               |
| `plugins/aphrodite/layout_schema.json`                   | `CB3-01 →C +guard` _(amendment 4)_                | Both                                | The **machine-readable layout contract** (schema_version 1) the self-heal module heals toward - the single reference for what lives where (`~/.hermes/aphrodite` = runtime home; plugin dir = pure loader, forbidden contents). `+guard`: encodes the pure-loader invariant; when missing/malformed, self-heal degrades to report-only. `D:{layout_schema.json} --Phase A--> C:{layout_schema.json'}` · `C:{layout_schema.json} --Phase B--> D:{layout_schema.json''}`                                      | Resumes the **layout contract process**: loaded by `layout_check._load_schema()`; drives dir creation, forbidden-content moves, symlink repair, and quarantine rules at startup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `plugins/aphrodite/README.md`                            | `DB3-01 →C` _(amendment 5)_                       | Both                                | Ships on Current (README is on the ship list); doc fixes/badge bumps transfer back in Phase B (selection benchmark: "README badge bumps"). Badge currently reads `v2.1.2` while plugin.yaml says `2.1.3` - a doc-level version indicator that rides releases (minor drift, cosmetic). `D:{README.md} --Phase A--> C:{README.md'}` · `C:{README.md} --Phase B--> D:{README.md''}`                                                                                                                            | Resumes the **install/architecture doc process**: one-command install (clone + manual symlink + enable), LLM provider config requirement, hooks/tools tables, hot-reload mechanics, directives location, the "plugin stays a pure loader / skills ship dev-side" statements.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `plugins/aphrodite/.gitattributes`                       | `GB3-01 →C` _(amendment 6)_                       | Both                                | Content-format contract (`* text=auto eol=lf`) for the plugin tree; transfers with the tree in both directions. `D:{.gitattributes} --Phase A--> C:{.gitattributes'}` · `C:{.gitattributes} --Phase B--> D:{.gitattributes''}`                                                                                                                                                                                                                                                                              | Resumes the **line-ending normalization process** on checkout - keeps the tree LF-clean so the byte-identical template guard and formatter contract hold.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `plugins/aphrodite/.gitignore`                           | `GB3-02 →C` _(amendment 7)_                       | Both                                | **Pure-loader guard**: excludes `binaries/`, `aphrodite.toml`, secrets, runtime artifacts (`ccr.db`, hotreload, logs) so they can never be committed into the plugin tree - the git-level half of the "runtime artifacts live in `~/.hermes/aphrodite`" ceremony rule. `D:{.gitignore} --Phase A--> C:{.gitignore'}` · `C:{.gitignore} --Phase B--> D:{.gitignore''}`                                                                                                                                       | Resumes the **tree-purity filter process**: keeps every downloader/loader output (binaries, config, DBs, `.hotreload/`) untracked and out of the release snapshot.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `plugins/aphrodite/tests/test_dylib_candidates.py`       | `TD3-01 ∅` _(amendment 8)_                        | Development ONLY                    | **Never crosses** (∅): tests are Development's job - Phase A keeps them out of Current, Phase B explicitly KEEPS them (unstage/discard `D tests/...` entries). Stdlib unittest (no pytest dep).                                                                                                                                                                                                                                                                                                             | Resumes the **issue-5 regression test** (shallow install paths must not raise IndexError in `_dylib_candidates`); loads the shim via `exec_module` without triggering registration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `plugins/aphrodite/tests/test_hotreload_cleanup.py`      | `TD3-02 ∅` _(amendment 9)_                        | Development ONLY                    | Same ∅ rule - never ships, Development keeps it in Phase B. Stdlib unittest.                                                                                                                                                                                                                                                                                                                                                                                                                                | Resumes the **hotreload reaper test**: dead-PID copies reaped, live-PID generations trimmed to newest, cache relocatable outside the plugin tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `plugins/aphrodite/tests/test_perf_probe.py`             | `TD3-03 ∅` _(amendment 10)_                       | Development ONLY                    | Same ∅ rule. Pytest-only by design (benchmark probe needs `pytest.skip`); excluded from the stdlib-unittest trio deliberately.                                                                                                                                                                                                                                                                                                                                                                              | Resumes the **performance probe**: N=100 compress→retrieve round trips over the REAL dylib (never a stub), p50/p95 latency report, p95 < 5s pathological bound, byte-identity asserted per sample; skips when no real dylib exists, fails loud when a present dylib is broken.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `plugins/aphrodite/tests/test_reaper_prefix_contract.py` | `TD3-04 ∅` _(amendment 11)_                       | Development ONLY                    | Same ∅ rule. Stdlib unittest.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Resumes the **issue-4 cross-platform reaper contract test**: dead-PID tombstones reaped for all three dylib name forms (`.dylib`/`.so`/`.dll`), unrelated prefixes NEVER reaped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `plugins/aphrodite/tests/test_windows_process_state.py`  | `TD3-05 ∅` _(amendment 12)_                       | Development ONLY                    | Same ∅ rule. Pytest (fixtures + monkeypatch), hermetic via `APHRODITE_HOME`→tmp + `APHRODITE_NO_AUTO_DOWNLOAD=1` + atexit guard.                                                                                                                                                                                                                                                                                                                                                                            | Resumes the **Windows multi-home load-path tests**: second shim exec reuses the mapped handle (no PermissionError on locked DLL), hot-reload still works across shim copies, `_start_proxy` pre-launch health probe (skips only on CONFIRMED aphrodite answer on both ports, never on foreign 200).                                                                                                                                                                                                                                                                                                                                                                            |

### 1.2 Not-tracked / artifact rows (not counted in files_classified)

| Path                              | HPC code                                     | Note                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `plugins/aphrodite/binaries/**`   | `A-3-01 @R` _(proposed, amendment 13)_       | **Runtime artifact, never source**: gitignored, absent from this checkout, populated by download.sh/ps1 into the canonical home `~/.hermes/aphrodite/binaries` - and layout self-heal actively MOVES any copy that appears inside the plugin dir back to the runtime home (or warn+skips in a checkout). Exists only at runtime on the user side, never in the Tag. |
| `plugins/aphrodite/directives/**` | **ABSENT by ceremony rule** _(amendment 14)_ | `directives/` was REMOVED from the plugin: directives now live in the binary's `builtin_directives` (core crate, layer 1) and are **materialized** into `~/.hermes/aphrodite/directives/` at startup by `aphrodite_hermes_materialize_directives`. The plugin dir stays a pure loader; the absence is a deliberate ceremony rule, not an omission.                  |
| skills (`.hermes/skills/`)        | **NOT shipped** _(amendment 15)_             | Skills are dev-side only (Development branch, never on Current). `register_skill` is deliberately never called - plugin.yaml comments, README, and `__init__.py` all state it. Skills travel with the monorepo's `.hermes/`, not with the submodule.                                                                                                                |

---

## 2. Notable ceremony interactions

1. **BINARY_VERSION last-bump rule** (`MC3-01 +tag+guard`): the manifest is a LIVE distribution pointer -
   both downloaders build `releases/download/Aphrodite%2Fv{V}` from it and `_check_version` handshakes it
   against the loaded dylib. Bumping it before the release assets exist breaks every download, so it is
   bumped LAST in any ceremony (after all code/config fixes, immediately before the tag). It also pairs
   **two independent release lines**: binary `Aphrodite/v1.4.3` vs plugin `v2.1.3` - the plugin's cadence
   and the binary's cadence are decoupled by design and this one file couples them.
2. **Submodule-first ordering (bottom-up)**: `plugins/aphrodite` syncs BEFORE the parent in BOTH directions -
   Phase A1 (plugin snapshot + commit + push) precedes A2 (parent), Phase B1 precedes B2. The parent gitlink
   must reference the plugin's tip, so the plugin is always the first process resumed and the last settled.
3. **Plugin gitlink float** (`+float`): the parent's 160000 gitlink entry for the submodule is floated to the
   released plugin tip at Action 11 (currently `497671b` = v2.1.2-37-g497671b on Development). The gitlink is
   branch-owned identity (restored per branch at Action 10/7) - the submodule's own tree is shared content;
   the pointer that floats is parent-side and owned by the release-infra/identity partner.
4. **plugin.yaml version rides the release** (`MB3-01 +bump +tag`): plugin tag `v2.1.3` exists; the manifest
   version is bumped on Development pre-A0, carried in the A1 squash snapshot, and the plugin is tagged FIRST
   (`v2.1.3` on S-Current, then `Aphrodite/v1.4.3` on P-Current). The plugin has NO CI of its own - the tag
   is its release marker.
5. **Template drift guard** (`+guard`, verified live): `crates/aphrodite/templates/__init__.py` is
   **byte-identical** to this `__init__.py` (cmp verified, 43467 B both). setup.rs embeds the template via
   `include_str!` and a drift-guard test (`test_hermes_plugin_shim_template_matches_live`) asserts equality.
   Ceremony rule: format the PLUGIN first, then `cp` to the template - the plugin copy is the source of truth;
   `ruff.toml`/`rustfmt.toml` ignore the templates dir for the same reason. The template is the crate's
   embedded copy of the loader (TAXONOMY `MB1-05 +guard`).
6. **skills/directives NOT shipped** (deliberate absence rules): skills live dev-side in `.hermes/skills/`
   (Development-only, never on Current); directives moved into the binary's `builtin_directives` and are
   materialized to the user-data home at startup. Both keep the plugin a pure loader - nothing in the
   submodule carries agent-behavior content across the boundary.
7. **Layout self-heal is the runtime mirror of the ceremony**: `layout_check.py` + `layout_schema.json`
   enforce "plugin dir = pure loader" on the user side (forbidden: `binaries/`, `aphrodite.toml`, `ccr.db`
   in the plugin dir; forbidden: plugin-source files in the runtime home) - the same separation the release
   ceremony enforces on the git side. A dev checkout is detected and left alone (developers manage the
   checkout), which is exactly the Development-vs-Current split made operational.
8. **Downloader guard around the last-bump pointer**: both downloaders run a strict semver regex
   (`^[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.]+)?$`) that rejects a malformed BINARY_VERSION (e.g. a stray
   `1|1.3.9` line-number artifact) before a bad release URL can be built - the plugin's own defense-in-depth
   around rule #1. `__init__._ensure_binaries` pins `BINARY_DIR` explicitly so an ambient env var can't
   redirect the fetch back into the plugin tree.
9. **Tests ∅ in both directions**: the 5 test files never cross - Phase A excludes them from Current
   (test-free line), Phase B keeps them on Development (unstage `D tests/...`). Development is the only
   place the plugin's loader process is verified (stdlib-unittest trio + 2 pytest probes).

---

## 3. Cross-cutting observations

- **15 tracked files, zero CI**: the submodule ships no workflows - its only "CI" is the parent's
  Development-side gates plus the stdlib-unittest trio that runs without pytest. The plugin's release
  marker is the tag `vX.Y.Z` itself (RELEASE-METHODOLOGY PART 4).
- **The halted-process framing is literal here**: `__init__.py` is explicitly designed to be resumed
  multiple times per process (one `exec_module` per Hermes home) with process-global state in a
  `sys.modules` holder module - opening the file N times resumes one loader, not N.
- **Two version lines, one coupling file**: `plugin.yaml` (2.1.3, plugin cadence) and `BINARY_VERSION`
  (1.4.3, binary cadence) are deliberately decoupled; BINARY_VERSION is the only thing that pairs them,
  which is exactly why it receives the special last-bump +guard ceremony attention.
- **Pure-loader invariant is triple-enforced**: `.gitignore` (git level), `layout_schema.json` +
  `layout_check.py` (runtime level), and the release methodology's ship/never-ship table (ceremony level)
  all enforce "the plugin tree holds no runtime artifacts and no agent-behavior content".
- **README badge drift observed**: badge `v2.1.2` vs manifest `2.1.3` - cosmetic, rides the release;
  candidate for the Phase B "README badge bumps" selection.
- **Submodule HEAD 37 commits past tag**: `v2.1.2-37-g497671b` on Development, clean tree - the last 37
  commits (layout self-heal relocation, skills-shipping removal, phantom-gitlink drop) are unreleased work
  awaiting the next Phase A snapshot.

---

## 4. Amendments (proposed, NOT yet written to TAXONOMY.md)

| #    | Amendment                                                                                        | Status   |
| ---- | ------------------------------------------------------------------------------------------------ | -------- |
| 1    | `MB3-01` plugin.yaml →C +bump +tag                                                               | proposed |
| 2    | `SB3-01` download.sh →C +guard                                                                   | proposed |
| 3    | `SB3-02` download.ps1 →C +guard                                                                  | proposed |
| 4    | `CB3-01` layout_schema.json →C +guard                                                            | proposed |
| 5    | `DB3-01` README.md →C                                                                            | proposed |
| 6    | `GB3-01` .gitattributes →C                                                                       | proposed |
| 7    | `GB3-02` .gitignore →C                                                                           | proposed |
| 8-12 | `TD3-01…05` tests/** ∅                                                                           | proposed |
| 13   | `A-3-01 @R` binaries/** runtime artifacts (untracked, not counted)                               | proposed |
| 14   | directives/ absence = ceremony rule (directives travel with the binary, materialized at startup) | proposed |
| 15   | skills NOT shipped = ceremony rule (dev-side `.hermes/skills/`, Development-only)                | proposed |

## 5. Verification

15 tracked files classified in this pass (not-tracked / artifact rows excluded from the count); `prettier --check` passes.
