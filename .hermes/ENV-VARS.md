# Aphrodite Environment Variable Reference

Moved to [`docs/config/env-vars.md`](../docs/config/env-vars.md) - that copy
is kept current with the actual code (file:line-anchored per var) and marks
which vars are genuinely wired vs documented-but-unread. This file used to
duplicate that table by hand and had drifted onto 8 different default values
than the code actually uses (report 07 F5) - keeping one copy instead of two
is the fix, not a better sync process.
