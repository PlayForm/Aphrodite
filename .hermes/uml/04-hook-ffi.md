# 04 - Hermes Hook & FFI Path

The Hermes plugin path: Hermes fires a hook → Python ctypes shim
(`crates/aphrodite/templates/__init__.py`, mirrored byte-identical at
`plugins/aphrodite/__init__.py` via setup.rs) resolves the dylib fresh each
call → C ABI in `crates/aphrodite-hermes/src/lib.rs`
(`aphrodite_hermes_call_hook`, panic-guarded) → core `aphrodite` crate hook
bodies. The `pre_llm_call` arm is the **choke point**: bridge, core
`hooks::pre_llm_call`, and the `context_engine_pre_llm` tool all converge on
the single assembler `flow::build_turn_context`, so directive injection can
never fork between paths again (bug class 01-F3).

FFI declarations come from the **generated `_bindings.py`** (cbindgen →
ctypesgen, regenerated by `crates/aphrodite-hermes/build.rs` whenever the C ABI
surface changes): `bind_to(dylib)` replays restype/argtypes onto the plugin's
own CDLL handle (the generated module never loads a library itself). When the
artifact is absent (fresh checkout / `aphrodite setup` install) or fails to
apply, the shim degrades to the manual `_manual_ffi_setup` fallback; on every
load, `_REQUIRED_VOID_P` asserts each pointer-returning export reads at full
64-bit width (a truncated default `c_int` restype would SIGSEGV in `_read_str`

- harden item 3). `_probe_dylib` smoke-tests each unique dylib path in a
  subprocess first, so a faulting image degrades to a graceful 'plugin disabled'
  instead of killing the gateway.

## Tool-result / terminal-output transform (bridge path)

```mermaid
sequenceDiagram
    autonumber
    participant H as Hermes host
    participant PY as __init__.py _hook_dispatch
    participant DL as _load_dylib (probe + mtime hot-reload)
    participant CJ as _call_json (forced restype + same-handle free)
    participant CH as aphrodite_hermes_call_hook (hermes/lib.rs:297)
    participant UW as tools::unwrap_hermes_result (tools.rs:68)
    participant TI as hooks::transform_tool_result_with_meta (hooks.rs:69)
    participant IN as transform_tool_result_inner (hooks.rs:121)
    participant ST as inline_store + recent_markers (AphroditeState)
    participant RF as replacement_from (hermes/lib.rs:108)

    H->>PY: transform_tool_result(tool_name, result, status, error_*, duration_ms)
    PY->>DL: _load_dylib()  (smoke-tested fresh copy if mtime changed)
    DL->>DL: _probe_dylib in subprocess once per unique path -<br/>SIGSEGV stays in the child, never the gateway
    PY->>CJ: aphrodite_hermes_call_hook("transform_tool_result", args_json)
    CJ->>CJ: fn.restype = c_void_p (forced - default c_int truncates 64-bit ptr)
    CJ->>CH: extern "C" (guarded - panic → {"error":...})
    CH->>UW: unwrap_hermes_result(tool_content) → (classify_content, type)
    Note over UW: unwraps {output,exit_code}/{diff}/{error}/{matches}/{content}<br/>ORIGINAL content still hashed verbatim - only type/preview affected
    CH->>TI: transform_tool_result_with_meta(state, content, tool, classify, meta)
    TI->>IN: inner pipeline
    IN->>ST: record_tool_event_from_meta (always - telemetry)
    IN->>IN: gates: empty / essential-tool / self-tool / below tool_threshold
    alt over threshold, compressible
        IN->>ST: inline_store_put(hash, content) · record_marker
        IN-->>CH: {compressed:true, hash, type, size, preview, marker}
    else skipped
        IN-->>CH: {compressed:false, reason}
    end
    CH->>RF: replacement_from(result)
    RF-->>CJ: marker string  (or Value::Null if not compressed)
    CJ->>CJ: _read_str(ptr) - NULL guarded BEFORE the cast (harden item 2)
    CJ->>CJ: aphrodite_hermes_free_string(ptr) - through the SAME dylib<br/>handle that produced the pointer (F4: never a reloaded image)
    CJ-->>PY: json
    PY-->>H: string → Hermes swaps tool output for marker (or leaves it on null)
```

`_call_json` frees every pointer through the exact `dylib` object that
allocated it, never through a cached/reloaded handle - benign today while both
generations use the system allocator, undefined behavior the day a custom
global allocator lands (F4).

## pre_llm_call - the directive-injection choke point

```mermaid
flowchart TD
    subgraph entries["Three entry points converge"]
      A["bridge: call_hook('pre_llm_call') (hermes/lib.rs:297)"]
      B["core FFI: aphrodite_dispatch('pre_llm_call') → hooks::pre_llm_call (hooks.rs:437)"]
      C["context_engine_pre_llm tool (tools.rs:516)"]
    end
    A --> D["flow::build_turn_context(state, est_bytes) (flow.rs:64)"]
    B --> D
    C --> D
    D --> E["directives = build_directive_context (directives.rs:157) - NEVER dropped"]
    D --> F["nudges = render_nudges (flow.rs:132) - ≤2 inline [nudge:…], NEVER dropped"]
    D --> G["recall = session::catalog_summary (session.rs:66) - droppable FIRST"]
    E --> H["assemble top-down under state.flow_budget_chars (default 4000)"]
    F --> H
    G --> H
    H --> I{"joined len > budget?"}
    I -->|yes| J["pop last section (recall block) - directives+nudges survive"]
    I -->|no| K["emit context"]
    J --> I
    K --> L["injected into model turn:<br/>[directives]…<br/>[nudge:…]<br/>[recall]…<br/>retrieve hint"]
```

`post_llm_call` (hooks.rs:462) is the mirror choke point on both paths:
`archive_turn` (last marker of the turn → `conv_index`) → `next_turn` (turn++)
→ `flow::purge_expired_nudges` (after the counter advances, so a ttl=1 nudge
renders exactly once) → `poll_worker::expire_stale_tasks`.

## Parity: bridge path vs core (handle-based) path

```mermaid
flowchart LR
    subgraph py["Python shim (templates/__init__.py)"]
      T["_make_handler → dispatch_tool"]
      HK["_hook_dispatch → call_hook"]
      FFI["_configure_ffi: generated _bindings.py<br/>bind_to(dylib) → manual fallback →<br/>_REQUIRED_VOID_P assertion"]
    end
    subgraph bridge["aphrodite-hermes (process-global 1 state)"]
      DT["aphrodite_hermes_dispatch_tool (lib.rs:235)"]
      CH2["aphrodite_hermes_call_hook (lib.rs:297)"]
      SG["aphrodite_hermes_get_schemas / get_hooks<br/>(lib.rs:489,498) - schemas + hook list"]
    end
    subgraph core["aphrodite core (handle-based ABI)"]
      DP["aphrodite_dispatch (lib.rs:536)"]
      IH["hooks::{transform_*, pre_llm_call, post_llm_call}"]
    end
    T --> DT
    HK --> CH2
    DT --> R["tools::dispatch registry (tools.rs:19)"]
    CH2 --> IH
    DP --> IH
    R --> SH["with_shared → AphroditeState (OnceLock)"]
    IH --> SH
    FFI -.->|restype/argtypes replayed| DT
    FFI -.->|restype/argtypes replayed| CH2
    Note1["pre_llm_call: bridge + core + context_engine_pre_llm<br/>ALL call flow::build_turn_context - single assembler"]
    style Note1 fill:#eef
```

Both `guarded()` wrappers (core `lib.rs:141`, hermes `lib.rs:225`) catch panics
across the FFI boundary and return `{"error":"internal error: panicked…"}`
instead of aborting the process. `_with_meta` variants exist only on the bridge
because the handle-based core ABI has no Hermes `status`/`error_type`
telemetry to plumb. The bridge no longer exposes any skill export
(`aphrodite_hermes_list_skills` was removed with the skills-as-shipped design);
the plugin registers tools and hooks only, and skills live dev-side in
`.hermes/skills/`.

## Key call sites

- Python: `_load_dylib` (probe + hot-reload), `_call_json` (forced restype,
  same-handle free), `_hook_dispatch`, `register` - `crates/aphrodite/templates/__init__.py:492,636,1167,1106` (mirror: `plugins/aphrodite/__init__.py`)
- Python FFI setup: `_configure_ffi` / `_manual_ffi_setup` / `_ensure_ffi_argtypes` / `_REQUIRED_VOID_P` - `crates/aphrodite/templates/__init__.py:428,458,480,56`
- `_probe_dylib` (subprocess smoke test) / `_read_str` (NULL guard) - `crates/aphrodite/templates/__init__.py:373,621`
- `aphrodite_hermes_call_hook` (bridge hooks) / `replacement_from` - `crates/aphrodite-hermes/src/lib.rs:297,108`
- `tools::unwrap_hermes_result` / `tools::dispatch` - `crates/aphrodite-hermes/src/tools.rs:68,19`
- `hooks::transform_tool_result_inner` / `pre_llm_call` / `post_llm_call` - `crates/aphrodite/src/hooks.rs:121,437,462`
- `flow::build_turn_context` (assembler) - `crates/aphrodite/src/flow.rs:64`
- core C ABI `guarded` / `aphrodite_dispatch` - `crates/aphrodite/src/lib.rs:141,536`
