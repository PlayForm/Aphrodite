**[Compare Aphrodite/v1.4.3...Aphrodite/v1.4.4](https://github.com/PlayForm/Aphrodite/compare/Aphrodite/v1.4.3...Aphrodite/v1.4.4)**

## Aphrodite v1.4.4 💋 Plugin v2.1.3

### Summary

v1.4.4 is a setup hotfix release: `cargo install` setup now completes even
when the core `libaphrodite_hermes` dylib cannot be produced (the dylib is
best-effort in the setup path - the plugin downloads it at runtime from the
release assets instead), the plugin shim template is synced byte-identical
with the live formatter-aligned `__init__.py`, the ruff formatter config is
explicit with the templates directory excluded from reformatting, the README
plugin badge moves to v2.1.3, and the v1.4.3 release notes are finalized on
the release line. Binary `1.4.3 → 1.4.4`, plugin `2.1.2 → 2.1.3`.

### Changes

- **Fix (setup)**: core `libaphrodite` dylib is best-effort so `cargo install`
  setup completes (`ee25ec2`).
- **Chore (plugin)**: plugin submodule bumped to `f8f4cdf` - ruff reformat of
  `__init__.py` (`2f3b461`); README plugin version updated to v2.1.3
  (`b478e95`).
- **Chore (formatter)**: plugin shim template synced with the live
  (100-style); explicit ruff formatter config + templates exclude; explicit
  VSCode formatter bindings (`c5cbed8`).
- **Docs**: v1.4.3 release notes finalized (test-free line, latest deps,
  directives, formatter sync) (`e616edc`).

### Verification

Retrospective synthesis - commit range `Aphrodite/v1.4.3..Aphrodite/v1.4.4`
(6 commits) analyzed via `git log` + `git diff --stat`; the release was built
and tested by CI at the time (Check.yml on Current, test-free line).

### What Ships

| Artifact                             | Platform                                    |
| ------------------------------------ | ------------------------------------------- |
| `aphrodite-aarch64-apple-darwin`     | macOS ARM64                                 |
| `aphrodite-x86_64-apple-darwin`      | macOS Intel                                 |
| `aphrodite-x86_64-unknown-linux-gnu` | Linux x86_64                                |
| `aphrodite-x86_64-pc-windows-msvc`   | Windows x86_64                              |
| Plugin v2.1.3                        | Hermes (standalone repo `Aphrodite-Hermes`) |

### Links

- **Full Changelog**: https://github.com/PlayForm/Aphrodite/compare/Aphrodite/v1.4.3...Aphrodite/v1.4.4
- **CHANGELOG.md**: [CHANGELOG.md](CHANGELOG.md)
- **Plugin**: https://github.com/PlayForm/Aphrodite-Hermes
- **Headroom Fork**: https://github.com/PlayForm/Headroom