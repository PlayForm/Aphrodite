// Fixture module for struct_extract benches
import { readFile } from 'node:fs/promises';

export interface Shape0 { id: number; name: string; }
export class Widget0 {
  constructor(private id: number = 0) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load0(path: string): Promise<Shape0> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape0;
}

export interface Shape1 { id: number; name: string; }
export class Widget1 {
  constructor(private id: number = 1) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load1(path: string): Promise<Shape1> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape1;
}

export interface Shape2 { id: number; name: string; }
export class Widget2 {
  constructor(private id: number = 2) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load2(path: string): Promise<Shape2> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape2;
}

export interface Shape3 { id: number; name: string; }
export class Widget3 {
  constructor(private id: number = 3) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load3(path: string): Promise<Shape3> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape3;
}

export interface Shape4 { id: number; name: string; }
export class Widget4 {
  constructor(private id: number = 4) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load4(path: string): Promise<Shape4> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape4;
}

export interface Shape5 { id: number; name: string; }
export class Widget5 {
  constructor(private id: number = 5) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load5(path: string): Promise<Shape5> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape5;
}

export interface Shape6 { id: number; name: string; }
export class Widget6 {
  constructor(private id: number = 6) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load6(path: string): Promise<Shape6> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape6;
}

export interface Shape7 { id: number; name: string; }
export class Widget7 {
  constructor(private id: number = 7) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load7(path: string): Promise<Shape7> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape7;
}

export interface Shape8 { id: number; name: string; }
export class Widget8 {
  constructor(private id: number = 8) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load8(path: string): Promise<Shape8> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape8;
}

export interface Shape9 { id: number; name: string; }
export class Widget9 {
  constructor(private id: number = 9) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load9(path: string): Promise<Shape9> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape9;
}

export interface Shape10 { id: number; name: string; }
export class Widget10 {
  constructor(private id: number = 10) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load10(path: string): Promise<Shape10> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape10;
}

export interface Shape11 { id: number; name: string; }
export class Widget11 {
  constructor(private id: number = 11) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load11(path: string): Promise<Shape11> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape11;
}

export interface Shape12 { id: number; name: string; }
export class Widget12 {
  constructor(private id: number = 12) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load12(path: string): Promise<Shape12> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape12;
}

export interface Shape13 { id: number; name: string; }
export class Widget13 {
  constructor(private id: number = 13) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load13(path: string): Promise<Shape13> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape13;
}

export interface Shape14 { id: number; name: string; }
export class Widget14 {
  constructor(private id: number = 14) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load14(path: string): Promise<Shape14> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape14;
}

export interface Shape15 { id: number; name: string; }
export class Widget15 {
  constructor(private id: number = 15) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load15(path: string): Promise<Shape15> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape15;
}

export interface Shape16 { id: number; name: string; }
export class Widget16 {
  constructor(private id: number = 16) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load16(path: string): Promise<Shape16> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape16;
}

export interface Shape17 { id: number; name: string; }
export class Widget17 {
  constructor(private id: number = 17) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load17(path: string): Promise<Shape17> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape17;
}

export interface Shape18 { id: number; name: string; }
export class Widget18 {
  constructor(private id: number = 18) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load18(path: string): Promise<Shape18> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape18;
}

export interface Shape19 { id: number; name: string; }
export class Widget19 {
  constructor(private id: number = 19) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load19(path: string): Promise<Shape19> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape19;
}

export interface Shape20 { id: number; name: string; }
export class Widget20 {
  constructor(private id: number = 20) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load20(path: string): Promise<Shape20> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape20;
}

export interface Shape21 { id: number; name: string; }
export class Widget21 {
  constructor(private id: number = 21) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load21(path: string): Promise<Shape21> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape21;
}

export interface Shape22 { id: number; name: string; }
export class Widget22 {
  constructor(private id: number = 22) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load22(path: string): Promise<Shape22> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape22;
}

export interface Shape23 { id: number; name: string; }
export class Widget23 {
  constructor(private id: number = 23) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load23(path: string): Promise<Shape23> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape23;
}

export interface Shape24 { id: number; name: string; }
export class Widget24 {
  constructor(private id: number = 24) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load24(path: string): Promise<Shape24> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape24;
}

export interface Shape25 { id: number; name: string; }
export class Widget25 {
  constructor(private id: number = 25) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load25(path: string): Promise<Shape25> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape25;
}

export interface Shape26 { id: number; name: string; }
export class Widget26 {
  constructor(private id: number = 26) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load26(path: string): Promise<Shape26> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape26;
}

export interface Shape27 { id: number; name: string; }
export class Widget27 {
  constructor(private id: number = 27) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load27(path: string): Promise<Shape27> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape27;
}

export interface Shape28 { id: number; name: string; }
export class Widget28 {
  constructor(private id: number = 28) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load28(path: string): Promise<Shape28> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape28;
}

export interface Shape29 { id: number; name: string; }
export class Widget29 {
  constructor(private id: number = 29) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load29(path: string): Promise<Shape29> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape29;
}

export interface Shape30 { id: number; name: string; }
export class Widget30 {
  constructor(private id: number = 30) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load30(path: string): Promise<Shape30> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape30;
}

export interface Shape31 { id: number; name: string; }
export class Widget31 {
  constructor(private id: number = 31) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load31(path: string): Promise<Shape31> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape31;
}

export interface Shape32 { id: number; name: string; }
export class Widget32 {
  constructor(private id: number = 32) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load32(path: string): Promise<Shape32> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape32;
}

export interface Shape33 { id: number; name: string; }
export class Widget33 {
  constructor(private id: number = 33) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load33(path: string): Promise<Shape33> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape33;
}

export interface Shape34 { id: number; name: string; }
export class Widget34 {
  constructor(private id: number = 34) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load34(path: string): Promise<Shape34> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape34;
}

export interface Shape35 { id: number; name: string; }
export class Widget35 {
  constructor(private id: number = 35) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load35(path: string): Promise<Shape35> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape35;
}

export interface Shape36 { id: number; name: string; }
export class Widget36 {
  constructor(private id: number = 36) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load36(path: string): Promise<Shape36> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape36;
}

export interface Shape37 { id: number; name: string; }
export class Widget37 {
  constructor(private id: number = 37) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load37(path: string): Promise<Shape37> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape37;
}

export interface Shape38 { id: number; name: string; }
export class Widget38 {
  constructor(private id: number = 38) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load38(path: string): Promise<Shape38> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape38;
}

export interface Shape39 { id: number; name: string; }
export class Widget39 {
  constructor(private id: number = 39) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load39(path: string): Promise<Shape39> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape39;
}

export interface Shape40 { id: number; name: string; }
export class Widget40 {
  constructor(private id: number = 40) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load40(path: string): Promise<Shape40> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape40;
}

export interface Shape41 { id: number; name: string; }
export class Widget41 {
  constructor(private id: number = 41) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load41(path: string): Promise<Shape41> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape41;
}

export interface Shape42 { id: number; name: string; }
export class Widget42 {
  constructor(private id: number = 42) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load42(path: string): Promise<Shape42> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape42;
}

export interface Shape43 { id: number; name: string; }
export class Widget43 {
  constructor(private id: number = 43) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load43(path: string): Promise<Shape43> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape43;
}

export interface Shape44 { id: number; name: string; }
export class Widget44 {
  constructor(private id: number = 44) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load44(path: string): Promise<Shape44> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape44;
}

export interface Shape45 { id: number; name: string; }
export class Widget45 {
  constructor(private id: number = 45) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load45(path: string): Promise<Shape45> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape45;
}

export interface Shape46 { id: number; name: string; }
export class Widget46 {
  constructor(private id: number = 46) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load46(path: string): Promise<Shape46> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape46;
}

export interface Shape47 { id: number; name: string; }
export class Widget47 {
  constructor(private id: number = 47) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load47(path: string): Promise<Shape47> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape47;
}

export interface Shape48 { id: number; name: string; }
export class Widget48 {
  constructor(private id: number = 48) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load48(path: string): Promise<Shape48> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape48;
}

export interface Shape49 { id: number; name: string; }
export class Widget49 {
  constructor(private id: number = 49) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load49(path: string): Promise<Shape49> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape49;
}

export interface Shape50 { id: number; name: string; }
export class Widget50 {
  constructor(private id: number = 50) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load50(path: string): Promise<Shape50> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape50;
}

export interface Shape51 { id: number; name: string; }
export class Widget51 {
  constructor(private id: number = 51) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load51(path: string): Promise<Shape51> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape51;
}

export interface Shape52 { id: number; name: string; }
export class Widget52 {
  constructor(private id: number = 52) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load52(path: string): Promise<Shape52> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape52;
}

export interface Shape53 { id: number; name: string; }
export class Widget53 {
  constructor(private id: number = 53) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load53(path: string): Promise<Shape53> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape53;
}

export interface Shape54 { id: number; name: string; }
export class Widget54 {
  constructor(private id: number = 54) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load54(path: string): Promise<Shape54> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape54;
}

export interface Shape55 { id: number; name: string; }
export class Widget55 {
  constructor(private id: number = 55) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load55(path: string): Promise<Shape55> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape55;
}

export interface Shape56 { id: number; name: string; }
export class Widget56 {
  constructor(private id: number = 56) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load56(path: string): Promise<Shape56> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape56;
}

export interface Shape57 { id: number; name: string; }
export class Widget57 {
  constructor(private id: number = 57) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load57(path: string): Promise<Shape57> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape57;
}

export interface Shape58 { id: number; name: string; }
export class Widget58 {
  constructor(private id: number = 58) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load58(path: string): Promise<Shape58> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape58;
}

export interface Shape59 { id: number; name: string; }
export class Widget59 {
  constructor(private id: number = 59) {}
  render(depth: number): string { return `w${this.id}:${depth}`; }
}

export async function load59(path: string): Promise<Shape59> {
  const raw = await readFile(path, 'utf8');
  return JSON.parse(raw) as Shape59;
}
