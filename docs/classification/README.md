# Classification

How the compression pipeline labels content before storage: the vendored
base classifier, the Aphrodite semantic refinement layer, and the per-type
threshold groups that decide how aggressively each type is compressed.

- [Content Types](content-types.md) - the complete taxonomy: 30 content types,
  detection order per stage, threshold groups, and the preview shapes each
  type produces
